// Sibling module imported by `imported-const-resolution.gts`. Exports a
// string constant the consumer references via `{{ROUTE_DIR}}`.
export const ROUTE_DIR = 'bogus';
export const OTHER_THING = 'unused';
